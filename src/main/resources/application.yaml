server:
  port: 8082

spring:
  datasource:
    url: jdbc:h2:c:/fiapcervejaria;DB_CLOSE_ON_EXIT=FALSE
    username: fiap
    password: fiap
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true